import 'package:flutter/material.dart';

class SpeakerProfilesPage extends StatelessWidget {
  const SpeakerProfilesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Speaker Profiles')),
      body: const Center(child: Text('Speaker Profiles Page')),
    );
  }
}
