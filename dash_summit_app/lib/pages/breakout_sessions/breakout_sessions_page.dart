import 'package:flutter/material.dart';

class BreakoutSessionsPage extends StatelessWidget {
  const BreakoutSessionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Breakout Sessions')),
      body: const Center(child: Text('Breakout Sessions Page')),
    );
  }
}
